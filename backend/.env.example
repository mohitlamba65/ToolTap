# ── LLM Inference Provider ─────────────────────────────────────────────────────
# Supported: "gemini" | "openai" | "github" | "ollama"
MODEL_PROVIDER=github

# ── GitHub Models (recommended — no rate limits with PAT) ──────────────────────
# Browse available models: https://github.com/marketplace/models
GITHUB_TOKEN=your_github_pat_here
GITHUB_BASE_URL=https://models.github.ai/inference
GITHUB_MODEL=openai/gpt-4.1-mini

# ── OpenAI (direct) ────────────────────────────────────────────────────────────
# MODEL_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# ── Google Gemini ──────────────────────────────────────────────────────────────
GOOGLE_API_KEY=your_google_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# ── Ollama (local) ─────────────────────────────────────────────────────────────
# MODEL_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_NUM_THREADS=8
OLLAMA_MODEL=qwen3:4b

# ── Embedding Provider ─────────────────────────────────────────────────────────
# Supported: "gemini" | "openai" | "github"
EMBEDDING_PROVIDER=openai
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ── Voice & Audio Providers ────────────────────────────────────────────────────
# Provider options: gemini | openai | github
TRANSCRIPTION_PROVIDER=openai
GEMINI_TRANSCRIPTION_MODEL=gemini-2.5-flash
OPENAI_TRANSCRIPTION_MODEL=whisper-1

# Text-To-Speech (TTS) options: gemini | openai
TTS_PROVIDER=openai
GEMINI_TTS_MODEL=gemini-2.5-flash-tts-preview
GEMINI_TTS_VOICE=Kore
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TTS_VOICE=coral

# ── Action Tools ───────────────────────────────────────────────────────────────
TAVILY_API_KEY=your_tavily_api_key_here
OPENWEATHER_API_KEY=your_openweather_api_key_here

# ── Mailgun Engine ─────────────────────────────────────────────────────────────
MAILGUN_API_KEY=your_mailgun_api_key_here
MAILGUN_DOMAIN=your_mailgun_domain_here
MAILGUN_BASE_URL=https://api.mailgun.net
MAILGUN_SENDER_EMAIL=your_sender_email@domain.com

# ── WhatsApp API ───────────────────────────────────────────────────────────────
WHATSAPP_PROVIDER=meta
WHATSAPP_API_URL=https://graph.facebook.com/v25.0
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
# Optional fallback if no number is saved in Settings
WHATSAPP_API_TOKEN=your_whatsapp_token
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
# Same value you paste into Meta webhook configuration
WHATSAPP_VERIFY_TOKEN=tooltap_verify_token
# Public origin for webhook copy (ngrok / production). If empty, derived from the request.
PUBLIC_BASE_URL=https://your-public-host.example
# 32-byte AES-256-GCM key: 64 hex chars, or 32-char utf8, or base64 that decodes to 32 bytes
INTEGRATION_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
WHATSAPP_DISPLAY_NUMBER=+910000000000
WEBHOOK_URL=

# ── Twilio sandbox (optional) ──────────────────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# ── LangSmith Tracing (Optional — leave false; tracing adds per-node latency) ──
LANGSMITH_TRACING=false
LANGSMITH_ENDPOINT=https://apac.api.smith.langchain.com
LANGSMITH_API_KEY=your_langsmith_api_key_here
LANGSMITH_PROJECT=agent-cli

# ── PostgreSQL + pgvector ──────────────────────────────────────────────────────
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=tooltap
POSTGRES_PASSWORD=tooltap_secret
POSTGRES_DB=tooltap
POSTGRES_URL=postgresql://tooltap:tooltap_secret@localhost:5433/tooltap?sslmode=disable
# 1536 = OpenAI text-embedding-3-small / GitHub; 768 = Gemini gemini-embedding-001
EMBEDDING_DIMENSIONS=1536
